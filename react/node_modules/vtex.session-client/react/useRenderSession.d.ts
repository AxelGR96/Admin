import { Session } from './SessionTypes';
interface SessionState {
    loading: boolean;
    session: Session | undefined;
    error: boolean | undefined;
}
declare function useRenderSession(): SessionState;
export default useRenderSession;
