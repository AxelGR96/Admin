import { QueryHookOptions } from 'react-apollo';
import type { Session } from './SessionTypes';
interface Data {
    session: Session;
}
interface Variables {
    items?: string[];
}
declare function useFullSession(options?: Omit<QueryHookOptions<Data>, 'ssr'>): import("react-apollo").QueryResult<Data, Variables>;
export default useFullSession;
