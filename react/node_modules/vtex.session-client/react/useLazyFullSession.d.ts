import { QueryHookOptions } from 'react-apollo';
import type { Session } from './SessionTypes';
interface Data {
    session: Session;
}
interface Variables {
    items?: string[];
}
declare function useLazyFullSession(options?: QueryHookOptions<Data, Variables>): import("@apollo/react-hooks").QueryTuple<Data, Record<string, any>>;
export default useLazyFullSession;
