import { MutationFunctionOptions, QueryHookOptions } from 'react-apollo';
import type { Session } from './SessionTypes';
interface Data {
    updateSession: Session;
}
interface Variables {
    fields: Record<string, string | number>;
    items?: string[];
}
declare function useUpdateSession(options?: QueryHookOptions<Data, Variables>): (mutationOptions: MutationFunctionOptions<Data, Variables>) => Promise<unknown>;
export default useUpdateSession;
