import { QueryHookOptions } from 'react-apollo';
import type { Session } from './SessionTypes';
interface Data {
    updateSession: Session;
}
interface Variables {
    fields: Record<string, string | number>;
    items?: string[];
}
declare function useUpdateSessionInline(options?: QueryHookOptions<Data, Variables>): import("react-apollo").MutationTuple<Data, Variables>;
export default useUpdateSessionInline;
